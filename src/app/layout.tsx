import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Boudoir Shoot Ellen - Professional Boudoir Photography",
  description: "Professional boudoir photography services by Ellen. Elegant, intimate, and empowering photography sessions in a comfortable and private setting.",
  keywords: "boudoir photography, professional photographer, intimate photography, portrait photography, Ellen photographer, elegant photography",
  authors: [{ name: "Ellen" }],
  robots: "index, follow",
};

export const viewport = {
  width: "device-width",
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
